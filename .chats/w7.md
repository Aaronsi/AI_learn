# Insititutions

## 探索
帮我研究一下市面上关于使用AI进行slides生成的工具，尤其是Manus和 NOtebookLM的 slide功能。探索它实现的原理。另外，探索如果使用 google 最新推出的 nana banana pro 来做 slides 生成 (思考：根据文本生成图片，把所有图片以幻灯片的形式连起来播放，就构成了 slides, 类似 NotebookLM里的 slides 生成，要求：图片的视觉风格要统一，用户可以提供一个视觉风格图片或者文字描述) 。

根据 @specs/w7/genslide.png 的内容，仔细阅读并思考，生成一个 ./specs/w7/0001-prd.md 的PR。要求：使用中文。这个 app 是一个本地运行的单页 app，使用 nano banana pr 生成图片slides，可以以走马灯的形式全屏播出。后端使用 Python，前端使用 Typescript。

prd需求需要修改：
1.图片生成使用 火山方舟的 Doubao-Seed-1.8 模型，模型ID为doubao-seed-1-8-251228，相应的API KEY 也帮忙改完整；
2.对于侧边栏 slide，选中并拖拽可以改变顺序；
3.文本内容变化后，如果中没有对应文本hash的图片，在主图片区域下放一个按钮，用户点击可以生成新的图片。
4.outline.yml 中需要保存用户选择的风格图片。当第一次打开时，如果没有风格图片，需要有个popup，用户可以输入一段文字，生成两个图片，让用户选择，用户选中的作为 slides的风格，以后生成新的图片时，参考这张图片。

outline 里的style 包含：prompt 和image，不要仅保存 style_image

## 生成 design spec
根据./specs/w7/0001-prd.md 和 @specs/w7/genslide.png 的内容，生成一个 design spec，放在 ./specs/w7/0002-design-spec.md 文件中。
要求:使用中文。注意所有前端所需的 API 接口要定义清楚。整体项目的目录结构也要定义清楚，后端代码层次清晰,API/业务、存储要保持清晰的边界。

## Nano banana pro 命令
google ai SDK 使用  https：//ai.google.dev/../../image-generation, 其中核心代码：
...
这里 nano banana pro的模型名：gemini-3-pro-image-preview，更新 design spec

检查 ./specs/w7/0002-design-spec.md 并确保deps中的版本都为最新版

### design spec review

帮我review ./specs/w7/0002-design-spec.md 文件。之后仔细阅读 review 的结果，思考是否合理，然后相应地更新 ./specs/w7/0002-design-spec.md 文件。

## 生成目录结构
根据 ./specs/w7/0002-design-spec.md 的内容，生成项目的空的目录结构。先不要生成代码。在 backend/ 和 frontend/ 目录下分别生成 CLAUDE.md 文件，内容考虑：

- 所使用语言框架的 best practices
- 架构设计遵循的原则：SOLID/YAGNI/KISS
- 代码的组织结构
- 并发处理
- 错误处理和日志处理

## 编写代码

根据 ./specs/w7/0001-prd.md , @specs/w7/002-design-spec.md 和@specs/w7/genslide.png 中的内容，启动 python 和 typescript 两个 agent 分别撰写后端和前端的代码，代码放在 ./w7/gen-slide下。

## 页面风格

使用浅黄色 / 红褐色的水彩画风格，走可爱卡通路线，主要角色是一只黄色的可爱的鸭子，类似 motherduck的风格。


doubao-seed-1-8-251228
https://ark.cn-beijing.volces.com/api/v3
7f8b2fe9-899b-4a31-ab36-5380387b1cec

根据 ./specs/w7/0001-prd.md , @specs/w7/002-design-spec.md 和@specs/w7/genslide.png 中的内容，优化刚开始打开的页面交互，目前交互如下./w7/gen-slide/error_images/page_error.png，非常不友好。整体页面框架在超出浏览器大小后，显示滚动条。并通过提示引导用户怎么使用。

根据 ./specs/w7/0001-prd.md , @specs/w7/002-design-spec.md 中的内容,think ultra hard, 严格按照@specs/w7/genslide.png 图片中的布局来优化界面交互。目前的页面布局如图 ./w7/gen-slide/error_images/page_error.png和./w7/gen-slide/error_images/page_error1.png，左边的slide列表跑到了中间，而且空间太大了。而选中左边某一个slide后，右边的预览界面都看不到了。请仔细参考@specs/w7/genslide.png 图片中的布局来进行页面布局和交互优化。
左边的slide列表高度超出浏览器高度后，需要有滚动条滚动来查看到所有的slide。另外，按照@specs/w7/genslide.png中的设计，头部的logo，slides题头和播放按钮应该显示在头部一行，其中logo和slides题头靠左显示，播放按钮靠右显示。请优化。

按照@specs/w7/genslide.png中的设计，头部的logo，slides题头和播放按钮应该显示在头部一行，其中logo和slides题头靠左显示，播放按钮靠右显示。目前是挤在了左上角一堆，请优化。


根据 ./specs/w7/0001-prd.md , @specs/w7/002-design-spec.md 中的需求和设计,think ultra hard, 按照./specs/w7/genslide.png中的原型设计,进行如下优化：
1.目前头部的logo，slides题头，播放按钮都挤在了左上角一起，如图：./w7/gen-slide/error_images/目前头部页面位置.png。按照genslide.png中的设计应该是logo和slides题头靠左显示，播放按钮靠右显示。
2.目前中间左边的slide列表没有滚动条，无法看到所有的slides。如图：./w7/gen-slide/error_images/中间左边slide列表没有滚动条&右边slide预览框没有显示.png。按照genslide.png中的设计应该是有一个列表显示所有的slide，如果超出了slide列表框高度，需要使用滚动条来滚动实现能看到所有的slide。
3.目前中间右边的slide预览框无法并排显示在slide列表右边。如图：./w7/gen-slide/error_images/中间右边slide预览框被挤到了页面最下面。按照genslide.png中的设计应该是并排显示在slide列表右边。
4.目前第一次打开时，风格图片的设置被挤在了最底下。如图：slide样式选择应该通过popup弹出框来设置而不是显示在最下面。实际需求为：当第一次打开时，如果没有风格图片，需要有个popup，用户可以输入一段文字，生成两个图片，让用户选择，用户选中的作为 slides的风格，以后生成新的图片时，参考这张图片。
另外，头部，中间左边slide列表，中间slide预览框都需要在浏览器中一屏全部显示出来，不能超出屏幕；除了slide列表中存在多个slides一屏显示不下的时候，需要有滚动条显示下所有slides供选择。


根据 ./specs/w7/0001-prd.md , @specs/w7/002-design-spec.md 中的需求和设计,think ultra hard, 按照./specs/w7/genslide.png中的原型设计,进行如下优化：
1.目前头部的logo，slides题头，播放按钮都挤在了左上角一起，如图：./w7/gen-slide/error_images/目前头部页面位置.png。按照genslide.png中的设计应该是logo和slides题头靠左显示，播放按钮靠右显示。
2.目前中间左边的slide列表没有滚动条，无法看到所有的slides。如图：./w7/gen-slide/error_images/中间左边slide列表没有滚动条&右边slide预览框没有显示.png。按照genslide.png中的设计应该是有一个列表显示所有的slide，如果超出了slide列表框高度，需要使用滚动条来滚动实现能看到所有的slide。
另外，头部，中间左边slide列表，中间slide预览框都需要在浏览器中一屏全部显示出来，不能超出屏幕；除了slide列表中存在多个slides一屏显示不下的时候，需要有滚动条显示下所有slides供选择。

目前前端界面有很大问题：
1. 没有使用 design-tokens.css 请确保使用定义好的 design tokens
2. 请再次阅读 ./specs/w7/genslide.jpg，页面结构应该跟它一致。比如：
- 侧边栏每个slide后面（下一个slide前面）的位置会显示一条横向，回车可以添加新的slide。
- 每个slide删除的按钮应该放在右上方。
- 双击slides 会打开一个比较大的 popup，用户可以输入内容。

重新阅读 ./specs/w7/0001-prd.md 中的需求，最主要是 6. 风格图片选择 章节，根据 @specs/w7/002-design-spec.md 设计，在已有代码基础上补充首次打开流程的实现。目前首次打开会报错，如图：./w7/gen-slide/error_images/第一次访问报错.png

重新阅读 ./specs/w7/0001-prd.md 中的需求，最主要是 6. 风格图片选择 章节，根据 @specs/w7/002-design-spec.md 设计，检查代码实现，目前首次打开会报错，如图：D:\develop\AI_learn\w7\gen-slide\error_images\第一次访问报错.png

请将图片存储路径从 ./w7/gen-slide/backend/slides/ 改为./w7/gen-slide/slides/ 下

根据 ./specs/w7/0001-prd.md 需求文档中 6. 风格图片选择 章节，当第一次打开时，如果没有风格图片，需要有个popup，用户可以输入一段文字，生成两个图片，让用户选择，用户选中的作为 slides的风格，以后生成新的图片时，参考这张图片。同时根据 @specs/w7/002-design-spec.md 设计，在现有代码基础上，实现改需求。

阅读 ./specs/w7/0001-prd.md 需求文档和 ./specs/w7/002-design-spec.md 设计文档，解决第一次打开时，输入风格文字后，点击 生成风格候选图 文字，调用接口报错问题，具体错误如图。D:\develop\AI_learn\w7\gen-slide\error_images\调用Doubao-Seed-1.8模型报错.png


似乎Doubao-Seed-1.8模型不支持根据文字生成图片，帮我更换为doubao-seedream-4-5-251128模型好了。

具体可以参考 https://www.volcengine.com/docs/82379/1824121?lang=zh#8bc49063 该文档中的 图文生图（单图输入单图输出） 章节内容以及代码
doubao-seedream-4-5-251128

输入 风格文字 后，点击 生成风格候选图  文字，调用接口报错，请看图解决问题。


https://www.volcengine.com/docs/82379/1824121?lang=zh#8bc49063 文生组图 可以参考该api文档中  文生组图章节内容与代码；提示词+单张图生成多张图 可以参考该文档中  单张图生组图 章节内容与代码；根据api调用方式，解决截图中，在输入 风格文字 后，点击 生成风格候选图  文字，调用接口报错。

在输入 风格文字 后，点击 生成风格候选图  文字，生成的候选图充满了整个浏览器页面，请居中缩小到中心位置。


根据 ./specs/w7/0001-prd.md , ./specs/w7/002-design-spec.md 中的需求和设计,think ultra hard, 按照./specs/w7/genslide.png 中的原型设计,进行如下优化：
- 当第一次打开时，如果没有风格图片，弹出的选择风格图片popup样式错乱；需优化；同时将 生成风格候选图 文字改为按钮；
- 在点击 生成风格候选图 按钮后，弹出的 生成候选图片等待  页面缩小居中显示；
- 候选图片选择页面也要缩小居中显示；
- 用户选中的候选图片作为这个项目的 slides 风格，以后生成新的slide时，风格参考这张图片。

重新阅读 ./specs/w7/0001-prd.md 中的需求，最主要是 6. 风格图片选择 章节，并./specs/w7/genslide.png 中的原型设计，进行如下优化：
-用户选中的候选图片后，需要再图片上增加选中效果，而不是图片下面增加选中效果。
-在用户选中候选图片风格后，该图片需要作为这个项目的slides风格，以后生成新的slide时，风格需要参考这张图片。
-在用户选中候选图片风格后，需要关闭 生成风格候选图 的popup。

仔细看这个wireframe修复其他页面问题（缩略图位置），图片展示大小等

双击 slide 打开一个popup 允许用户修改 slide 文本


阅读 ./specs/w7/0001-prd.md 中的需求和./specs/w7/002-design-spec.md设计，按照./specs/w7/genslide.png 前端原型设计，检查现有代码，看是否实现了需求中的 3. 图片生成  功能。如果没有，请实现。

目前还存在以下问题：
1.在左边栏slides列表中的某个slide下输入文字并保存后，需要根据文字和风格图片生成新的图片，并将生成的图片显示在右边的预览区；同时将生成图片的缩略图更新到文字上面的slide中。
2.单击左边栏slides列表中的某个slide，需要将slide对应的图片显示在右边的预览区。

如图，目前还存在以下问题：
1.按照./specs/w7/genslide.png 前端原型设计，左边栏slides列表中的每个slide里面，上面显示缩略图，下面显示文字；不需要显示缩略图上面的黑色背景框框。
2.在slide里面的下面输入文字，并按Ctrl+Enter保存文字后，并没有根据文字和风格图片生成新的图片。

阅读 ./specs/w7/0001-prd.md 中的需求，./specs/w7/002-design-spec.md设计和./specs/w7/genslide.png 前端原型设计。检查目前页面截图，并修复以下问题：
1.slide里面不需要红色框选中的，缩略图上方的黑色框。
2.点击某个slide，应该将slide对应的图片显示在右侧中间的预览框中。
具体可以查看./specs/w7/genslide.png 前端原型设计


根据 ./specs/w7/genslide.png 前端原型设计，检查并修复以下问题：
1.左侧slides列表中，slide 上边距与下边距太高，需要改小点；
2.add slide 文字与按钮需要在左侧slides列表中水平居中；
3.左边选中一个slide后：
3.1 在右侧中间的slide预览框中，图片要在现有区域框中显示完整，目前只能显示图片的上半部分。
3.2 在右侧下面的image History中，多张图片要上下高度一致。当前选中的图片，通过在图片右上角增加✅️角标来表示为当前选中图片。并且图片下面的文字要显示为生成图片时的文字，宽度不够可以换行显示。

根据./w7/gen-slide/下项目代码分别生成如下 ascii chart:
1. 前端架构
2. 后端架构
3. 数据流图
4. 前端技术组
5. 后端技术栈
并与./specs/w7/0001-prd.md 中的需求，./specs/w7/002-design-spec.md整体设计和./specs/w7/genslide.png 前端原型设计 对比检查，代码是否完整实现了 需求和设计中的内容。
将以上内容输出到./specs/w7/0003-architecture-output.md中。

根据 ./w7/gen-slide/backend/ 下面的代码使用 doubao-genai 的情况，构建一个使用 doubao-genai 调用 doubao-seedream-4-5-251128 生成图片（可以有reference图片）的skill。然后进一步探索网上的资料（比如：https://www.volcengine.com/docs/82379/1824121?lang=zh#8bc49063）和本地已经安装的 doubao-genai 包的源码，让使用skill 的 agent 能很快上手生成各种 image。生成在当前项目目录下；

这个 skill 的目的是当做新的项目的时候，知道如何使用 doubao-genai 库使用 doubao-seedream-4-5-251128。目前只支持 python，未来支持更多语言。

我把skill 放在./.claude/skills 下了。现在请将 python 的内容放在 references/python.md 下，这样 skill 可以在使用 python 的情况时，progressively地加载python内容。为以后添加更多语言支持留有余地。

现在请将skill中 python 的内容放在 references/python.md 下，这样 skill 可以在使用 python 的情况时，progressively地加载python内容。为以后添加更多语言支持留有余地。

