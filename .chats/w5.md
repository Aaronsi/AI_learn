# Instructions

## MCP研究

帮我深度研究目前业界使用mcp的主要方向以及成功案例 (e.g. playwright-mcp), 并指导我该如何使用 mcp 来扩展 agent 能力，哪些东西用mcp 做，哪些应该用 cli / claude skill 做，撰写一个详细的报告。

## 构建 mcp server

主要需求是在Python 下面创建一个Postgres 的mcp：用户可以给特定自然语言描述的查询的需求，然后mcp server 根据需求来返回一个SQL或者返回这个查询的结果。mcp的服务器在启动的时候，应该读取它都有哪些可以访问的数据库，并且缓存这些数据库的schema：了解每一个数据库下面的都有哪些 table/view/types/index 等等，然后根据这些信息以及用户的输入去调用DeepSeek的大模型 (DeepSeek-V3.2) 来生成 SQL。之后mcp server 应该来校验这个sql只允许查询的语句，然后测试这个sql确保它能够执行并且返回有意义的结果：这里也可以把用户的输入生成sql以及返回的结果的一部分调用DeepSeek来确认这样可以确保他的结果是不是有意义。最后根据用户的输入是返回SQL还是返回SQL查询之后的结果来返回相应的内容。根据这些需求帮我构建一个详细的需求文档，先不要着急去做设计，等我review完这个需求文档之后，我们再来讨论设计。文档放在 ./specs/w5/0001-pg-mcp-prd.md文件中。

## 构建 pg-mcp 的设计文档
根据 ./specs/w5/0001-pg-mcp-prd.md 文档，使用 FastMCP, Asyncpg, SQLGlot, Pydantic 以及 openai 构建 pg-mcp 的设计文档，文档放在 ./specs/w5/0001-pg-mcp-design.md 文件中。think ultra hard

帮我深度研究SQLGlot,了解它的设计理念和支持的sql范围。

帮我review ./specs/w5/0002-pg-mcp-design.md 文件。之后仔细阅读 review 的结果，思考是否合理，然后相应地更新 ./specs/w5/0002-pg-mcp-design.md 文件。


根据 ./specs/w5/0002-pg-mcp-design.md 文档， 构建 pg-mcp 的实现计划， think ultra hard, 文档放在 ./specs/w5/0004-pg-mcp-impl-plan.md 文件中。

帮我review ./specs/w5/0004-pg-mcp-impl-plan.md 文件，之后仔细阅读 review 的结果，思考是否合理，并将建议写入  ./specs/w5/0005-pg-mcp-impl-plan-review.md 文件。

按照 ./specs/w5/0005-pg-mcp-impl-plan-review.md 文件， 仔细思考是否合理和必要， 并将合理必要的建议相应地更新到 ./specs/w5/0004-pg-mcp-impl-plan.md 文件中。



为 ./ w5/pg-mcp 生成 CLAUDE.md. 要求：代码要符合 python best practice / idomatic python, 符合 SOLID/DRY 等设计思路，代码质量和测试质量要高，性能要好。

review skill

## 开发
根据 ./specs/w5/0004-pg-mcp-impl-plan.md 和 ./specs/w5/0002-pg-mcp-design.md 文档，使用 sub agent 完整实现 pg-mcp phase 0-1。代码放在 ./w5/pg-mcp 下。

根据 ./specs/w5/0004-pg-mcp-impl-plan.md 和 ./specs/w5/0002-pg-mcp-design.md 文档，使用 sub agent 完整实现 pg-mcp phase 2-3。代码放在 ./w5/pg-mcp 下。

根据 ./specs/w5/0004-pg-mcp-impl-plan.md 和 ./specs/w5/0002-pg-mcp-design.md 文档，使用 sub agent 完整实现 pg-mcp phase 4-5。代码放在 ./w5/pg-mcp 下。


根据 ./specs/w5/0004-pg-mcp-impl-plan.md 和 ./specs/w5/0002-pg-mcp-design.md 文档，使用 sub agent 完整实现 pg-mcp phase 6。内容放在 ./w5/pg-mcp 下。

帮我review 整个 ./w5/pg-mcp 下的代码，看其是否符合 ./specs/w5/0002-pg-mcp-design.md 和 ./specs/w5/0004-pg-mcp-impl-plan.md 要求，是否符合 python best practice / idomatic python，是否符合 SOLID/DRY 等设计要求，代码质量和测试质量和性能是否足够高。把review 结果写到 ./specs/w5/0006-pg-mcp-impl-code-review.md 文件。



## test plan
根据 ./specs/w5/0004-pg-mcp-impl-plan.md 和 ./specs/w5/0002-pg-mcp-design.md 文档，构建 pg-mcp 的测试计划。think ultra hard， 文档放在 ./specs/w5/0007-pg-mcp-test-plan.md 文件中。

之后调用 codex review skill 让 codex review ./specs/w5/0007-pg-mcp-test-plan.md 文件，并构建 
./specs/w5/0008-pg-mcp-test-plan-review.md 文件。 

deepseek_api_key: str = "sk-czxrtftrjlszminzjadicobjdpjbjqovxiotxzjpxajblqgm"
deepseek_base_url: str = "https://api.deepseek.com"

## 数据库准备
根据 ./specs/w5/0001-pg-mcp-prd.md 在 ./w5/pg-mcp/fixtures 下构建三个有意义的数据库，分别有少量，中等量级，以及大量的 table/views/types/index 等 schema, 且有足够多的数据。生成这三个数据库的 sql 文件，并构建 Makefile 来重建这些测试数据库。

根据 ./w5/pg-mcp/fixtures 下生成的三个数据库脚本，建立并测试这三个数据库，确保可用。

这是一个好的fix吗？为什么这么做？

确保代码 fmt/lint/test 都通过



根据这些fixture,假设用户要用自然语言提问，然后 pg-mcp 来生成相应的sql，帮我生成一个 test.md 的文档，里面包含各种对数据库内部数据的简单到复杂的提问