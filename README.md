# AI_learn

## w1 / Project Alpha

- 位置：`./w1/project-alpha`
- 后端：FastAPI + Postgres，路径 `backend/`
- 安装依赖：在 `backend` 目录执行 `uv sync`
- 迁移：`uv run alembic upgrade head`
- 运行：`uv run uvicorn app.main:app --reload`
- 环境变量示例见 `w1/project-alpha/.env.example`
